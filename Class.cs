using System;

public class Course
{
	public Course(int course_id_input, string name_input, string description_input, int teacher_id_input, int credit_hours_input, int max_size_input)
	{
        int course_id = course_id_input;
        string name = name_input;
        string description = description_input;
        int teacher_id = teacher_id_input;
        int credit_hours = credit_hours_input;
        int max_size = max_size_input;
        int current_size = 0;
	}
}
